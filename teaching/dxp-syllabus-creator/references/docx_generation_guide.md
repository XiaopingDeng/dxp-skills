# docx 生成详细指南

本文件补充 SKILL.md 中 docx 生成部分的详细规则。理论课程生成 docx 时必读。

## 目录
1. [页眉表（Table 0）替换规则](#1-页眉表table-0替换规则)
2. [评分标准表替换规则](#2-评分标准表替换规则)
3. [正文段落替换策略](#3-正文段落替换策略)
4. [多章节段落插入方法](#4-多章节段落插入方法)
5. [表格行数匹配](#5-表格行数匹配)
6. [列宽调整](#6-列宽调整)
7. [常见陷阱与解决方案](#7-常见陷阱与解决方案)

---

## 1. 页眉表（Table 0）替换规则

模板的页眉表已预填了大量标签文字（如"课程编码""课程名称""学分"等）。替换时遵循以下通用原则：

### 标签单元格识别与保护

**什么是标签单元格**：单元格文本是字段名称（如"课程编码""课程名称""学分""总学时""讲课""实验""上机""实践""考核方式""执行学期""先修课程""后续课程""课程类别""课程负责人"等）。

**保护规则**：
- 标签单元格保持原样，不覆写、不清空
- 通过检查单元格文本是否匹配已知标签来判断是否为标签单元格
- 若为标签单元格则跳过，否则为值单元格，可以写入数据

### 值单元格填充策略

**通用方法**：遍历页眉表所有行和列，对每个单元格：
1. 检查文本是否为已知标签 → 跳过
2. 检查文本是否为空或仅含占位符 → 写入对应的课程数据
3. 检查文本是否为预填的默认值（如考核方式默认描述）→ 可整体替换

### "课程负责人"区域绝对保护

模板中有一组"课程负责人"标签及其值区。这两个区域均不得以任何方式写入或清空——标签一旦被覆写会破坏模板结构，值区应留给用户手工填写。

**实现方法**：在代码中识别包含"负责人"文本的单元格及其同行右侧的所有单元格，全部跳过。

### 学时值独立写入

总学时、讲课、实验、上机、实践五个学时值必须各自独立写入纯数字值。禁止合并写入（如"64（讲课48学时，实验16学时）"）。每个值单元格只接受一个数字。

**原因**：培养方案中这五项是独立字段，分开显示便于教务系统录入和审核比对。

---

## 2. 评分标准表替换规则

评分标准表（平时作业/实验/期末考试的评分标准）是模板的核心表格。参考模板 `XD26010006 供配电与照明工程 课程教学大纲（2026版-参考模版）.docx` 采用 **4 级评分制**（不是 5 级制）。

### 表结构

每表结构为 3 列：课程目标 | 评价等级 | 评分标准。行数取决于课程目标数 × 4 个评分等级 + 1 行表头。

### 数据构造规则

构造数据数组时，将表头 `['课程目标', '评价等级', '评分标准']` 作为数组的第 0 个元素（索引 0），然后从索引 1 开始填充评分数据。这样在循环写入时不会覆盖模板已有的表头。

### 评价等级列（第 2 列）

只允许包含以下 4 种固定文本（参考模板的 4 级制）：

```
优
90-100
良
80-89
中/及格
60-79
差
0-59
```

注意：在模板中，等级名称和分数范围分两行显示（单元格内用换行符 `\n` 分隔），如 `优\n90-100`。

- 禁止在此列中出现任何评分标准描述文字
- 禁止在此列中出现课程目标编号、章节名称等其他信息
- 各行的等级单元格独立设置
- **批注要求**：如课程采用量化评分（非等级制），在评分标准中说明分数折算方式

### 评分标准列（第 3 列）

- 内容必须为针对当前课程的原创描述，使用课程专有术语
- 禁止直接复制模板中的原课程评分描述文字
- 每个等级对应一个评分标准描述，各等级之间体现递进差异

### 课程目标列（第 1 列）

- 目标编号必须使用"课程目标N"格式（含"课程"前缀）
- 与 Markdown 大纲中定义的课程目标编号完全一致
- 若课程目标数少于模板行数，多余行清空

---

## 3. 正文段落替换策略

### 核心原则

模板中的占位段落（如"……"或括号标注文字）必须用已生成的 Markdown 大纲中的课程专属内容完全替换。仅靠全局文本替换（如将原课程名替换为新课程名）无法将模板占位内容变为当前课程的实质性描述。

### 文本特征匹配

不依赖段落精确索引号（不同课程内容长度不同，索引会变化）。改用文本特征匹配：
1. 遍历 `doc.paragraphs`，检查每个段落的 `text.strip()`
2. 匹配特征文本（如"课程性质""课程任务""课程目标""基本要求""重点和难点""教学方法""建议教材"等）
3. 找到后，替换其后的第一个非空段落的文本

### 段落替换方法

```python
p.clear()        # 清除所有 runs
add_run_songti(p, new_text)  # 写入新文本，同时设置宋体字体
```

**关键**：`p.clear()` 后新创建的 run 不会继承模板的字体设置。必须使用显式设置宋体字体的辅助函数（如 `add_run_songti()`）。

### Footer 保护

模板末尾有"编写人/审核人/批准人/编写日期"四行 footer。做任何段落替换时，如果段落文本以这些关键词开头，则跳过所有替换操作。这些内容保持模板原样，由用户手工填写。

---

## 4. 多章节段落插入方法

### 问题

模板中第二节（教学内容）只有一组章节占位段落（1 个章节标题 + 基本要求 + 内容 + 重点和难点 + 内容 + 教学方法 + 内容）。当课程有多个章节时，需要为第 2 章及之后的章节插入新的段落。

### 解决方案

使用 `insert_paragraph_before(ref_p, text)` 辅助函数，在"……"占位段落之前插入新章节的段落：

```python
# 找到"……"占位段落
for i, p in enumerate(doc.paragraphs):
    if p.text.strip() == '……':
        # 在此之前插入所有额外章节
        for chapter in chapters[1:]:
            insert_paragraph_before(p, chapter['heading'], bold=True)
            insert_paragraph_before(p, "基本要求")
            insert_paragraph_before(p, chapter['req'])
            insert_paragraph_before(p, "重点和难点")
            insert_paragraph_before(p, chapter['key'])
            insert_paragraph_before(p, "教学方法")
            insert_paragraph_before(p, chapter['met'])
            insert_paragraph_before(p, "")  # 空行分隔
        # 清除"……"占位
        p.clear()
        break
```

### 实现原理

`insert_paragraph_before` 通过深拷贝参考段落的 XML 元素并清除其内容，然后用 `addprevious` 插入到目标位置之前。新段落的文本通过 `add_run_songti()` 写入，确保字体一致。

---

## 5. 表格行数匹配

参考模板 `XD26010006 供配电与照明工程 课程教学大纲（2026版-参考模版）.docx` 含 10 个表格。填充数据时确保数据行数不超过表格可用行数：

| 表 | 总行数 | 表头行 | 可用数据行 | 常见内容 |
|:---|:---:|:---:|:---:|:---|
| Table 0 页眉表 | 9 | — | — | 课程编码/名称/学分/学时/考核等 |
| Table 1 课程目标矩阵 | 4 | 1 | 3 | 3个课程目标（毕业要求/指标点/课程目标） |
| Table 2 教学内容对应 | 9 | 1 | 8 | 8个教学模块（含实验行） |
| Table 3 平时作业对应 | 5 | 1 | 4 | 4次作业 |
| Table 4 平时作业评分 | 13 | 1 | 12 | 3目标×4等级 |
| Table 5 随堂测试对应 | 4 | 1 | 3 | 3次测试 |
| Table 6 实验对应 | 5 | 1 | 4 | 4次实验 |
| Table 7 实验评分 | 5 | 1 | 4 | 1目标×4等级 |
| Table 8 期末考试对应 | 8 | 1 | 7 | 7个内容模块 |
| Table 9 达成度评价 | 6 | 2 | 4 | 3目标+合计行（7列宽表） |

- 若教学内容超过可用行数，合并章节到同一行
- 若课程目标数少于模板行数，多余数据行必须显式清空（`cell.text = ''`）
- 实验次数多于 4 次时，合并为 4 组写入
- **批注要求**：Table 2 的"课程教学内容"列须细化到知识点级别，每行 3-5 个核心知识点用"；"分隔

### Table 9 达成度评价表特殊结构

Table 9 为 6 行 × 7 列宽表，有**两行表头**（R0 为合并表头行，R1 为列名行）：
- R0: 毕业要求指标点 | 课程目标 | 考核与评价方式及成绩评价比例(%)（合并4列）| 总比例
- R1: 毕业要求指标点 | 课程目标 | 平时作业 | 随堂测试 | 实验 | 期末考试 | 总比例
- R2-R4: 3 个课程目标的数据行
- R5: 合计行

填充时从 R2 开始写入数据，R5 写入合计行。

### Table 2 学时分配规则

Table 2 中"学时"列的值之和必须等于总学时。**关键规则**：讲课学时和实验学时分离到不同行，不要在同一行中混合计入。

推荐做法：
- 前 N 行：各教学模块的**讲课学时**（纯数字）
- 最后 1 行：**实验学时合计**（纯数字）
- 所有行学时之和 = 总学时

避免的做法：将实验学时分散到各章节行中，同时又有一个实验合计行——这会导致学时重复计算。

---

## 6. 列宽调整

所有表格在内容替换完成后，根据内容长度调整列宽：

- **评分标准表**：评分标准列 ≥ 8cm，评价等级列 2-3cm，课程目标列 2.5-3.5cm
- **教学内容表**：课程教学内容列 ≥ 9cm（长文本需要足够宽度）
- **数据对应表**（作业/测验/实验/期末）：内容列 ≥ 7cm
- **页眉表**：有合并单元格，列宽基本固定，不做大幅调整

使用 `cell.width = Cm(xx)` 对每列的首行单元格设置宽度。

---

## 7. 常见陷阱与解决方案

### 陷阱 1：正文段落替换覆盖了错误的段落

**现象**：替换"课程性质"正文时，跳过了空段落，覆盖了"课程任务"或"课程目标"子标题。

**原因**：模板中子标题后的正文段落可能是空的。如果搜索"下一个非空段落"来替换，会跳过空段落并找到错误的段落。

**解决方案**：直接替换子标题后的第一个段落（索引 i+1），无论是否为空。不要搜索"下一个非空段落"。

### 陷阱 2：全局文本替换破坏出版社名称

**现象**：将"机械"替换为"计算机"后，"机械工业出版社"变成了"计算机工业出版社"。

**原因**：全局替换范围过大，替换单字而非完整课程名称。

**解决方案**：全局替换只替换**课程全名称**（如"供配电与照明工程"→"新课程名"），绝不替换单字或部分名称。

### 陷阱 3：中文字体不一致

**现象**：生成的 docx 中，某些段落字体不是宋体，与其他正文不一致。

**原因**：`p.clear()` 清除了所有 runs 及其格式，新 `add_run()` 创建的 run 没有字体设置。

**解决方案**：所有 `add_run()` 调用后，使用辅助函数显式设置 `font.name='宋体'` 和 `eastAsia='宋体'`。

### 陷阱 4：Python 字符串中的中文引号

**现象**：Python 脚本报 SyntaxError，指向包含中文引号的行。

**原因**：中文引号""（U+201C/U+201D）在某些编码下可能与 ASCII 双引号混淆。

**解决方案**：
- 使用 `\u201c` 和 `\u201d` 转义序列代替直接输入中文引号
- 或使用三引号字符串（`"""..."""`）
- 或在文件头部声明 `# -*- coding: utf-8 -*-`

### 陷阱 5：PowerShell 编码问题

**现象**：在 PowerShell 中运行 Python 脚本时，中文输出乱码。

**原因**：PowerShell 默认使用 GBK 编码，而 Python 脚本输出 UTF-8。

**解决方案**：
- 在脚本头部添加 `import sys, io; sys.stdout = io.TextIOWrapper(sys.stdout.buffer, encoding='utf-8')`
- 或使用 `chcp 65001` 切换 PowerShell 到 UTF-8
- 避免在 PowerShell 中内联 Python 代码（f-string 中的引号会与 PowerShell 冲突），改为写入 .py 文件后执行

### 陷阱 6：评分标准表表头被覆盖

**现象**：评分标准表的表头行变成了数据内容。

**原因**：循环写入数据时从 row 0 开始，覆盖了表头。

**解决方案**：构造数据数组时将表头作为第 0 个元素，循环从 row 0 开始写入，表头会被相同文本覆盖（无影响），数据从 row 1 开始正确填充。

---

## 8. 模板命名样式体系

参考模板 `XD26010006 供配电与照明工程 课程教学大纲（2026版-参考模版）.docx` 使用命名样式（Named Styles）控制格式。替换内容时**必须保留段落原有样式名**，不可更改 `paragraph.style` 属性。

### 样式清单

| 样式 ID | 样式名 | 字体（西文/中文） | 字号 | 对齐 | 加粗 | 用途 |
|:---|:---|:---|:---|:---|:---|:---|
| 2 | heading 1 | Times New Roman/黑体 | 22pt | — | 是 | 文档标题（P0） |
| 17 | 节标题 | Times New Roman/黑体 | 12pt | 两端对齐 | 是 | 六节主标题（一~六） |
| 1 | Normal | Times New Roman/宋体 | 10.5pt | 两端对齐 | 否 | 正文、子标题（运行级覆盖为 12pt） |
| 16 | 大纲正文 | Times New Roman/宋体 | 10.5pt | 两端对齐 | 否 | 正文段落 |
| 21 | 大纲强调 | 宋体/宋体 | — | — | 是 | 建议教材/学习资源标签 |
| 5 | Body Text Indent | 宋体/宋体 | — | — | 否 | 表格标题行 |
| 18 | 表格标题 | — | — | 居中 | 是 | 表格上方标题 |
| 9 | Normal (Web) | — | 12pt | 左对齐 | 否 | 教学方法正文（部分段落） |

### 替换时的样式保留规则

1. **不要更改 `paragraph.style`**：找到目标段落后，仅 `p.clear()` 清除文本，不修改样式属性
2. **运行级字体设置**：`add_run_songti()` 设置 `font.name='Times New Roman'` 和 `eastAsia='宋体'`（正文）或 `eastAsia='黑体'`（节标题）
3. **新插入段落复制样式**：`insert_paragraph_before()` 深拷贝参考段落的 XML 元素，自动继承样式；新插入的章节标题段落应复制"Normal"样式的加粗格式
4. **表格内文本**：使用 `set_cell_font()` 设置宋体字体，保持与模板表格一致

---

## 9. 模板批注编制要求

参考模板含 5 条批注，是编制方对大纲内容的具体要求。生成 docx 时不生成批注本身，但内容须遵循：

| 批注 | 位置 | 批注原文 | 生成要求 |
|:---|:---|:---|:---|
| 0 | 教学内容区域 | 教学内容应该具体明确，重点难点突出！课程思政要紧密结合课程内容，建议每门课至少选3个典型思政提要融入课程内容和教学方法。 | 各章基本要求≥3条用"了解/理解/掌握"；重点难点具体到概念名称；立德树人≥3个思政融入点与章节绑定 |
| 1 | 教学方法 | 涉及教学方法均为建议，可根据教学内容、教学手段实时更新、调整。 | 教学方法贴合课程实际，非生搬硬套，可灵活调整 |
| 2 | 表 2 | 教学内容和课程目标应根据实际情况对应，建议可以对应到章、节，建议细化到知识点。 | Table 2 教学内容列每行3-5个核心知识点，用"；"分隔 |
| 3 | 表 4 评分标准 | 此评分标准适用于按照等级划分的作业成绩；如果作业成绩已经量化，按照实际分数折算。同样适用于随堂测验。 | 4级制评分（优/良/中及格/差）；量化评分时说明折算方式 |
| 4 | 期末考试 | 期末考试如果采用闭卷、开卷考试，评分标准按照试卷评分标准；如果采用大作业、论文等无法具体量化的形式，需要给出相应的期末成绩评价等级标准。 | 根据考核形式给出对应评分方式；非闭卷须给等级评价标准 |
